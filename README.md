# predictive-weather-app
An end-to-end weather forecasting web application built with PyTorch and Streamlit. The app uses a trained Long Short-Term Memory (LSTM) neural network to model historical climate dynamics and predict multi-hour future trends for temperature, humidity, atmospheric pressure, and wind speed.
